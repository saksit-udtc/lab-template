# 📘 คู่มือการส่งงานผ่าน GitHub (ฉบับละเอียด)
> วิชา: ระบบปฏิบัติการเครื่องแม่ข่าย (31901-2002) | วิทยาลัยเทคนิคอุดรธานี | ปวส.

## 📋 ภาพรวมขั้นตอน
1. สมัคร GitHub Account
2. สร้าง repo ส่วนตัวจาก template ของครู
3. ติดตั้ง Git และ Clone repo ลงเครื่อง
4. เปิดไฟล์ pcap ด้วย Wireshark วิเคราะห์ และกรอกคำตอบ
5. Push คำตอบขึ้น GitHub
6. ดูคะแนนอัตโนมัติที่ tab Actions

---

## ส่วนที่ 1 — สมัคร GitHub Account
> ถ้ามี GitHub account แล้ว ข้ามไปส่วนที่ 2 ได้เลย

**ขั้นที่ 1:** เปิดเบราว์เซอร์ไปที่ https://github.com/signup

**ขั้นที่ 2:** กรอกข้อมูลลงทะเบียน
- Enter your email → ใส่อีเมลของคุณ
- Create a password → ตั้งรหัสผ่าน (อย่างน้อย 8 ตัว)
- Enter a username → ตั้งชื่อ user เช่น `somchai-6701001`

> 💡 แนะนำตั้งชื่อ username เป็น `ชื่อ-รหัสนักศึกษา` เพื่อให้ครูระบุตัวได้ง่าย

**ขั้นที่ 3:** ยืนยันอีเมล — เปิดอีเมลที่สมัครไว้ หาเมลจาก GitHub แล้วคลิกลิงก์ยืนยัน

---

## ส่วนที่ 2 — สร้าง Repo ส่วนตัวจาก Template

**ขั้นที่ 1:** เปิดลิงก์ template ของครู
```
https://github.com/saksit-udtc/lab-template
```

**ขั้นที่ 2:** กดปุ่มสีเขียว **"Use this template"** (แถวบนขวาของหน้า) → เลือก **"Create a new repository"**

**ขั้นที่ 3:** ตั้งค่า repo ใหม่
- Repository name → ตั้งชื่อเป็น `lab-ชื่อของคุณ` เช่น `lab-somchai`
- Public/Private → เลือก **Private**

> ⚠️ อย่าลืมเปลี่ยนเป็น Private ไม่งั้นคนอื่นเห็นคำตอบได้

**ขั้นที่ 4:** กด **"Create repository"** — URL ของคุณจะเป็น `https://github.com/YOUR_USERNAME/lab-somchai`

**ขั้นที่ 5:** เพิ่มครูเป็น Collaborator (สำคัญ!)
- ไปที่ **Settings** (แถบเมนูบน repo) → **Collaborators** ทางซ้าย → กด **Add people**
- ค้นหา `saksit-udtc` → กด **Add saksit-udtc to this repository**

> 💡 ถ้าไม่เพิ่ม ครูจะมองไม่เห็นงานของคุณ

---

## ส่วนที่ 3 — ติดตั้ง Git และ Clone Repo

### 3.1 ติดตั้ง Git (ทำครั้งเดียว)
1. ดาวน์โหลด https://git-scm.com/download/win → คลิก **64-bit Git for Windows Setup**
2. เปิดไฟล์ที่ดาวน์โหลด กด **Next** ไปเรื่อยๆ จนถึง **Install** แล้วรอ **Finish**
3. ตรวจสอบ: เปิด PowerShell พิมพ์ `git --version` ถ้าขึ้นเวอร์ชันแสดงว่าสำเร็จ
4. ตั้งค่าชื่อและอีเมล (ครั้งเดียว):
```
git config --global user.name "YOUR_NAME"
git config --global user.email "YOUR_EMAIL"
```

### 3.2 Clone Repo ลงเครื่อง
1. เปิด repo ของคุณบน GitHub → กดปุ่มสีเขียว **`< > Code`** → แถบ **HTTPS** → กด copy URL
2. เปิด PowerShell พิมพ์:
```
git clone URL_ที่คัดลอกมา
```
3. เข้าโฟลเดอร์:
```
cd lab-somchai
```

---

## ส่วนที่ 4 — กรอกคำตอบและส่งงาน

### 4.1 ดาวน์โหลดไฟล์ pcap
1. เปิด https://github.com/saksit-udtc/network-security-lab
2. เข้าโฟลเดอร์ lab ที่ได้รับมอบหมาย เช่น `lab01`
3. คลิกไฟล์ `.pcap` → กด **Download raw file**
4. เปิดด้วย Wireshark แล้ววิเคราะห์คำตอบ

### 4.2 กรอกคำตอบในไฟล์ YAML
1. เปิดโฟลเดอร์ `lab-somchai` ที่ clone มา → เข้าโฟลเดอร์ `answers`
2. เปิดไฟล์ `lab01.yml` ด้วย Notepad หรือ VS Code

ไฟล์หน้าตาแบบนี้:
```yaml
student_id: ""          ← ใส่รหัสนักศึกษา เช่น 6701001
student_name: ""        ← ใส่ชื่อ-นามสกุล เช่น สมชาย ใจดี

answers:
  q1_scanner_ip: ""     ← ใส่คำตอบของข้อ 1 ตรงนี้
  q1_target_ip: ""
  q2_port_22: ""        ← open / closed / filtered
```

ตัวอย่างหลังกรอกแล้ว:
```yaml
student_id: "6701001"
student_name: "สมชาย ใจดี"

answers:
  q1_scanner_ip: "10.10.60.50"
  q1_target_ip: "10.10.60.100"
  q2_port_22: "open"
```

> ⚠️ อย่าลบหรือเปลี่ยนชื่อ key (เช่น `q1_scanner_ip` ต้องคงเดิม)
> ⚠️ บันทึกไฟล์เป็น UTF-8 ถ้าใช้ Notepad เลือก Save as → Encoding: UTF-8

---

## ส่วนที่ 5 — Push คำตอบขึ้น GitHub

เปิด PowerShell ในโฟลเดอร์ repo แล้วพิมพ์ทีละบรรทัด:

```
cd lab-somchai
git add .
git commit -m "ส่งงาน lab01"
git push
```

ถ้า push ครั้งแรก GitHub อาจถามชื่อและรหัสผ่าน — ใช้ **Personal Access Token** แทน password:

### สร้าง Personal Access Token
1. GitHub → **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
2. กด **Generate new token (classic)**
3. ตั้งชื่อ เช่น `lab-token` เลือก scope: **repo**
4. กด **Generate token** แล้วคัดลอก token (แสดงครั้งเดียว!)
5. ใช้ token นี้แทน password ตอน `git push`

---

## ส่วนที่ 6 — ดูคะแนนอัตโนมัติ

1. เปิด repo ของคุณบน GitHub
2. คลิกแถบ **Actions** (แถบเมนูบน)
3. รอ 🟡 (กำลังตรวจ) เปลี่ยนเป็น ✅ (ตรวจเสร็จ) ประมาณ 1-2 นาที
4. คลิก workflow ล่าสุด → คลิก **grade** เพื่อดูผลละเอียด

ตัวอย่างผลที่เห็น:
```
✅ LAB01 — สมชาย ใจดี (6701001)
   คะแนน: 13.0/13.0 (100.0%)
     [✓] q1_scanner_ip: 10.10.60.50 (+1.5/1.5)
     [✓] q2_port_22: open (+0.75/0.75)
     [✗] q4_filter_open: ผิด (+0/2)
```

> 💡 ข้อที่ [✗] แสดงว่าคำตอบยังผิด กลับไปแก้ไขและ push ใหม่ได้เลย

---

## สรุปคำสั่ง Git ที่ใช้บ่อย

| คำสั่ง | ความหมาย |
|--------|----------|
| `git clone <url>` | ดาวน์โหลด repo ลงเครื่อง (ครั้งแรกครั้งเดียว) |
| `git add .` | เตรียมไฟล์ที่แก้ไขทั้งหมดเพื่อบันทึก |
| `git commit -m "ข้อความ"` | บันทึกการเปลี่ยนแปลงพร้อมข้อความ |
| `git push` | ส่งขึ้น GitHub |
| `git pull` | ดึงการเปลี่ยนแปลงจาก GitHub ลงเครื่อง |
| `git status` | ดูสถานะว่าไฟล์ไหนถูกแก้ไขบ้าง |

---

*ติดปัญหาให้ถามครูได้เสมอครับ/ค่ะ 🙏*
